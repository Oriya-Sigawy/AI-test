"""Personal Expense Tracker API — application package.

A FastAPI service over PostgreSQL in three thin layers (routers -> services ->
models), with Pydantic v2 schemas and JWT bearer auth. The package layout and
the responsibility of each module are described in
``specs/001-expense-tracker-api/plan.md`` (§Project Structure).
"""
